import {
  ADD_GOODS,
  GET_GOODS,
  CURRENT_GOODS,
  CLEAR_FILTER,
  GOODS_ERROR,
  DELETE_GOODS,
  UPDATE_GOODS,
  FILTER_GOODS,
} from '../types';

export default (state, action) => {
  switch (action.type) {
    case ADD_GOODS:
      break;

    default:
      break;
  }
};
